# persona

Validated coherent identity configuration for only the surfaces Brimp controls:
curl impersonation profile, request User-Agent/language, Navigator values,
locale/languages, viewport, and device-pixel ratio. Unknown fields—including
unimplemented fingerprint surfaces—are rejected. Resolution is deterministic
for the explicit seed and never guesses a profile from a User-Agent.
